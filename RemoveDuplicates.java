//4.	Write a program to remove the duplicate value of the array.
public class RemoveDuplicates {
    public static void main(String[] args) {
        int arr[] = { 1, 2, 4, 2, 1, 3 };

        for (int i = 0; i < arr.length; i++) {
            for (int j = i+1; j < arr.length; j++) {
                if (arr[i]==arr[j]) {
                    
                }
            }
        }
    }
}
